# License 管理模块实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现现场客户端 License 管理功能，包括申请、导入、校验，以及公司管理端的 License 生成功能。

**Architecture:**
- 后端新增 `api/license.py` 模块，处理 MAC 地址获取、platform_id 生成、AES 加解密、License 校验
- 路由分散在 `api/routes.py` 的 `handle_get` 和 `handle_post` 中
- 前端新增 License 申请页面（全屏覆盖）和系统管理 License 子页面

**Tech Stack:** Python (标准库 + PyCryptodome for AES), Vanilla JS, HTML/CSS

---

## 文件结构

```
api/license.py              # 新建：License 核心逻辑
api/routes.py               # 修改：添加 /api/license/* 和 /api/admin/license/* 路由
static/panels.js            # 修改：添加 License 管理面板
static/index.html           # 修改：添加 License 申请页面结构
static/style.css            # 修改：添加 License 页面样式
```

---

## Task 1: 后端 - License 核心模块

**Files:**
- Create: `api/license.py`

### 核心函数清单

```python
# 获取本机 MAC 地址
def get_mac_address() -> str:
    """获取本机第一个非_LOOPBACK MAC 地址，格式为 AA:BB:CC:DD:EE:FF"""

# 生成 platform_id
PLATFORM_ID_SALT = "hermes-webui-license"  # 固定盐值

def generate_platform_id(secret_key: str, mac_address: str) -> str:
    """根据 secret_key + MAC + 盐值生成 platform_id，格式：PLAT-XXXXXX"""

# 计算 MAC 哈希（用于防拷贝验证）
def compute_mac_hash(secret_key: str, mac_address: str) -> str:
    """计算 SHA256(secret_key + MAC) 用于存储验证"""

# 生成 License 字符串（公司端用）
def generate_license_string(secret_key: str, platform_id: str, mac_address: str, expires_at: str) -> str:
    """
    AES-256-CBC 加密
    明文 = platform_id + "|" + mac_address + "|" + expires_at
    返回 base64(encrypted)
    """

# 解密并解析 License（现场端用）
def decrypt_license_string(license_string: str, secret_key: str) -> dict | None:
    """AES 解密，返回 {"platform_id", "mac_address", "expires_at"} 或 None"""

# License 目录和文件路径
def get_license_dir(workspace: Path) -> Path:
    """返回 {workspace}/.license/"""

def get_license_config_path(workspace: Path) -> Path:
    """返回 {workspace}/.license/license.json"""

def get_secret_key_path(workspace: Path) -> Path:
    """返回 {workspace}/.license/secret_key"""

# License 配置读写
def load_license_config(workspace: Path) -> dict:
    """读取 license.json，不存在则返回默认结构"""

def save_license_config(workspace: Path, config: dict) -> None:
    """写入 license.json"""

# 初始化 License 配置
def init_license_config(workspace: Path) -> dict:
    """
    1. 检查 secret_key 是否存在，不存在则抛出异常
    2. 读取或创建 license.json
    3. 获取 MAC，生成 platform_id，计算 mac_hash
    4. 保存并返回配置
    """

# 校验 License 状态（页面访问时调用）
def check_license_status(workspace: Path) -> dict:
    """
    返回 {"activated": bool, "expires_at": str|null, "days_remaining": int|null, "status": str}
    status: "valid" | "expired" | "not_activated" | "copied"
    """

# 导入 License（激活）
def import_license(workspace: Path, license_string: str) -> dict:
    """
    1. 用 secret_key 解密 license_string
    2. 验证 platform_id 和 MAC 与本机一致
    3. 验证未过期
    4. 更新 license.json: activated=true, mac_hash, expires_at, imported_at
    返回 {"ok": bool, "error"?: str, "expires_at"?: str}
    """
```

---

## Task 2: 后端 - API 路由

**Files:**
- Modify: `api/routes.py` (在 handle_get 和 handle_post 中添加路由)

### GET 路由

在 `handle_get` 中添加（位置：在 `if parsed.path.startswith("/api/notes"):` 之前）:

```python
# License 状态
if parsed.path == "/api/license/status":
    from api.license import check_license_status, init_license_config
    try:
        init_license_config(SESSION_DIR)  # 确保已初始化
    except FileNotFoundError:
        return j(handler, {"error": "secret_key not found"}, status=500)
    status = check_license_status(SESSION_DIR)
    # 动态计算 platform_id
    from api.license import get_mac_address, generate_platform_id, get_secret_key_path
    mac = get_mac_address()
    secret_key = get_secret_key_path(SESSION_DIR).read_text().strip()
    status["platform_id"] = generate_platform_id(secret_key, mac)
    status["mac_address"] = mac
    return j(handler, status)
```

在 `handle_get` 中添加（公司管理端）:

```python
# License 列表（公司端）
if parsed.path == "/api/admin/license/list":
    from api.license import get_admin_license_list
    return j(handler, get_admin_license_list())
```

### POST 路由

在 `handle_post` 中添加:

```python
# 获取本机信息（用于申请）
if parsed.path == "/api/license/apply":
    from api.license import get_mac_address, generate_platform_id, get_secret_key_path
    mac = get_mac_address()
    secret_key = get_secret_key_path(SESSION_DIR).read_text().strip()
    return j(handler, {
        "platform_id": generate_platform_id(secret_key, mac),
        "mac_address": mac
    })

# 导入 License
if parsed.path == "/api/license/import":
    from api.helpers import require
    body = read_body(handler)
    license_string = require(body, "license_string")
    from api.license import import_license
    result = import_license(SESSION_DIR, license_string)
    if not result["ok"]:
        return j(handler, result, status=400)
    return j(handler, result)

# 公司端生成 License
if parsed.path == "/api/admin/license/generate":
    from api.license import generate_license_string, get_secret_key_path
    from api.helpers import require
    body = read_body(handler)
    platform_id = require(body, "platform_id")
    mac_address = require(body, "mac_address")
    expires_at = require(body, "expires_at")
    secret_key = get_secret_key_path(SESSION_DIR).read_text().strip()
    license_string = generate_license_string(secret_key, platform_id, mac_address, expires_at)
    from api.license import save_generated_license
    save_generated_license(platform_id, mac_address, expires_at)
    return j(handler, {"ok": True, "license_string": license_string})
```

---

## Task 3: 后端 - 公司端 License 列表存储

**Files:**
- Modify: `api/license.py` (添加管理端相关函数)

```python
# 公司端：已生成的 License 列表（内存存储，简单实现）
_generated_licenses: list[dict] = []

def save_generated_license(platform_id: str, mac_address: str, expires_at: str) -> None:
    """保存生成的 License 记录"""
    _generated_licenses.append({
        "platform_id": platform_id,
        "mac_address": mac_address,
        "expires_at": expires_at,
        "generated_at": time.strftime('%Y-%m-%dT%H:%M:%SZ', time.gmtime())
    })

def get_admin_license_list() -> dict:
    """返回已生成的 License 列表"""
    return {"licenses": _generated_licenses}
```

---

## Task 4: 前端 - License 申请页面

**Files:**
- Modify: `static/index.html` (在 body 末尾添加 License 申请页面 HTML)
- Modify: `static/style.css` (添加样式)

### HTML 结构

```html
<div id="licenseOverlay" class="license-overlay" style="display:none;">
  <div class="license-container">
    <h2>License 激活</h2>
    <p>请将以下信息发送给管理员以获取 License</p>
    <div class="license-info">
      <div class="license-field">
        <label>平台 ID:</label>
        <span id="licensePlatformId" class="license-value"></span>
        <button onclick="copyToClipboard('licensePlatformId')">复制</button>
      </div>
      <div class="license-field">
        <label>MAC 地址:</label>
        <span id="licenseMacAddress" class="license-value"></span>
        <button onclick="copyToClipboard('licenseMacAddress')">复制</button>
      </div>
    </div>
    <div class="license-import">
      <p>导入 License 文件：</p>
      <input type="file" id="licenseFileInput" accept=".lic,.txt">
      <button onclick="importLicense()">导入</button>
    </div>
    <p id="licenseError" class="license-error" style="display:none;"></p>
  </div>
</div>
```

### CSS 样式

```css
.license-overlay {
  position: fixed;
  top: 0; left: 0; right: 0; bottom: 0;
  background: rgba(0,0,0,0.9);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 9999;
}
.license-container {
  background: #1e1e1e;
  padding: 40px;
  border-radius: 12px;
  max-width: 500px;
  width: 90%;
}
.license-field {
  display: flex;
  align-items: center;
  gap: 10px;
  margin: 15px 0;
}
.license-value {
  font-family: monospace;
  background: #2d2d2d;
  padding: 8px 12px;
  border-radius: 4px;
  flex: 1;
}
.license-error {
  color: #ff6b6b;
  margin-top: 15px;
}
```

---

## Task 5: 前端 - JavaScript 逻辑

**Files:**
- Modify: `static/panels.js` (添加 License 相关函数)

### 需要添加的函数

```javascript
// 检查 License 状态并显示/隐藏 overlay
async function checkLicenseStatus() {
  try {
    const resp = await fetch('/api/license/status');
    const data = await resp.json();
    const overlay = document.getElementById('licenseOverlay');
    if (!data.activated || data.status === 'expired' || data.status === 'copied') {
      // 显示 License 申请页面
      document.getElementById('licensePlatformId').textContent = data.platform_id || 'N/A';
      document.getElementById('licenseMacAddress').textContent = data.mac_address || 'N/A';
      overlay.style.display = 'flex';
    } else {
      overlay.style.display = 'none';
    }
  } catch (e) {
    console.error('License check failed:', e);
  }
}

// 复制到剪贴板
function copyToClipboard(elementId) {
  const text = document.getElementById(elementId).textContent;
  navigator.clipboard.writeText(text);
}

// 导入 License
async function importLicense() {
  const fileInput = document.getElementById('licenseFileInput');
  const errorEl = document.getElementById('licenseError');
  errorEl.style.display = 'none';

  if (!fileInput.files.length) {
    errorEl.textContent = '请选择 License 文件';
    errorEl.style.display = 'block';
    return;
  }

  const file = fileInput.files[0];
  const text = await file.text();

  try {
    const resp = await fetch('/api/license/import', {
      method: 'POST',
      headers: {'Content-Type': 'application/json'},
      body: JSON.stringify({license_string: text.trim()})
    });
    const result = await resp.json();
    if (result.ok) {
      // 激活成功，刷新页面
      location.reload();
    } else {
      errorEl.textContent = result.error || '导入失败';
      errorEl.style.display = 'block';
    }
  } catch (e) {
    errorEl.textContent = '导入失败: ' + e.message;
    errorEl.style.display = 'block';
  }
}
```

### 在 boot.js 或 panels.js 初始化时调用 checkLicenseStatus

在 `panels.js` 末尾或合适的初始化位置添加：
```javascript
// 页面加载时检查 License
document.addEventListener('DOMContentLoaded', checkLicenseStatus);
```

---

## Task 6: 前端 - 系统管理 License 子页面

**Files:**
- Modify: `static/panels.js` (在系统管理面板中添加 License 管理 Tab)

### 添加 License 管理面板内容

在系统管理面板的 HTML 结构中添加：

```html
<div id="licenseAdminPanel" class="admin-sub-panel" style="display:none;">
  <h3>License 管理</h3>
  <div class="license-admin-info">
    <div class="info-row"><span>状态:</span><span id="adminLicenseStatus">-</span></div>
    <div class="info-row"><span>平台 ID:</span><span id="adminPlatformId">-</span></div>
    <div class="info-row"><span>MAC 地址:</span><span id="adminMacAddress">-</span></div>
    <div class="info-row"><span>过期时间:</span><span id="adminExpiresAt">-</span></div>
    <div class="info-row"><span>剩余天数:</span><span id="adminDaysRemaining">-</span></div>
    <div class="info-row"><span>导入时间:</span><span id="adminImportedAt">-</span></div>
  </div>
</div>
```

### 添加加载 License 信息的函数

```javascript
async function loadLicenseAdminInfo() {
  try {
    const resp = await fetch('/api/license/status');
    const data = await resp.json();
    document.getElementById('adminLicenseStatus').textContent = data.status || 'not_activated';
    document.getElementById('adminPlatformId').textContent = data.platform_id || '-';
    document.getElementById('adminMacAddress').textContent = data.mac_address || '-';
    document.getElementById('adminExpiresAt').textContent = data.expires_at || '-';
    document.getElementById('adminDaysRemaining').textContent = data.days_remaining !== null ? data.days_remaining + ' 天' : '-';
    document.getElementById('adminImportedAt').textContent = data.imported_at || '-';
  } catch (e) {
    console.error('Failed to load license info:', e);
  }
}
```

---

## Task 7: 测试

**Files:**
- Create: `tests/test_license.py`

### 测试用例

```python
def test_generate_platform_id():
    from api.license import generate_platform_id
    pid = generate_platform_id("test_secret", "AA:BB:CC:DD:EE:FF")
    assert pid.startswith("PLAT-")
    assert len(pid) == 11  # PLAT- + 6 chars

def test_compute_mac_hash():
    from api.license import compute_mac_hash
    hash1 = compute_mac_hash("secret", "AA:BB:CC:DD:EE:FF")
    hash2 = compute_mac_hash("secret", "AA:BB:CC:DD:EE:FF")
    assert hash1 == hash2
    assert len(hash1) == 64  # SHA256 hex

def test_license_encrypt_decrypt():
    from api.license import generate_license_string, decrypt_license_string
    secret = "my_secret_key"
    pid = "PLAT-123456"
    mac = "AA:BB:CC:DD:EE:FF"
    expires = "2027-06-30T23:59:59Z"

    license_str = generate_license_string(secret, pid, mac, expires)
    result = decrypt_license_string(license_str, secret)
    assert result is not None
    assert result["platform_id"] == pid
    assert result["mac_address"] == mac
    assert result["expires_at"] == expires

def test_license_decrypt_wrong_key():
    from api.license import generate_license_string, decrypt_license_string
    license_str = generate_license_string("secret1", "PLAT-123456", "AA:BB:CC:DD:EE:FF", "2027-06-30")
    result = decrypt_license_string(license_str, "wrong_secret")
    assert result is None
```

---

## Task 8: 集成测试

**Files:**
- Create: `tests/test_license_api.py`

### API 测试

```python
def test_license_status_endpoint(test_server):
    resp = requests.get(f"{test_server}/api/license/status")
    assert resp.status_code in (200, 500)  # 500 if no secret_key

def test_license_apply_endpoint(test_server):
    resp = requests.get(f"{test_server}/api/license/apply")
    # 需要先设置 secret_key 文件
    assert resp.status_code in (200, 500)

def test_license_import_invalid(test_server):
    resp = requests.post(f"{test_server}/api/license/import", json={"license_string": "invalid"})
    assert resp.status_code == 400
```

---

## 执行顺序

1. Task 1: 后端 - License 核心模块 (`api/license.py`)
2. Task 2: 后端 - API 路由 (`api/routes.py`)
3. Task 3: 后端 - 公司端 License 列表存储
4. Task 4: 前端 - License 申请页面 HTML/CSS
5. Task 5: 前端 - JavaScript 逻辑
6. Task 6: 前端 - 系统管理 License 子页面
7. Task 7: 单元测试
8. Task 8: 集成测试

---

**Plan complete.**