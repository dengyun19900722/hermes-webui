# ZK Changelog

## 2026-07-24

### RBAC 与会话权限

- 修复会话列表的 RBAC 可见性规则：除 `admin` 角色可查看全部会话外，普通用户仅能看到自己创建的会话或别人分享给自己的会话。
- `/api/sessions` 响应补充当前 RBAC 用户作用域信息，包括用户 ID、用户名、角色，用于前端按登录身份校验缓存。
- 修复普通用户能看到其他用户会话的问题，前端和后端都增加了 RBAC 作用域过滤。
- 修复 RBAC 用户新建空会话被清理、刷新后消失的问题，RBAC 归属的空会话不再被零消息孤儿清理逻辑误删。
- 修复新建会话覆盖旧会话、会话列表永远只有一个的问题，保留用户作用域内的会话行并正确合并本地乐观会话。

### 会话列表缓存与账号切换

- 增加登录用户身份作用域缓存键，账号切换、退出登录、登录成功后会清理旧用户的本地会话缓存。
- 会话列表刷新前同步 `/api/auth/status`，避免旧用户请求晚返回后覆盖新用户列表。
- 账号变化时废弃进行中的 `/api/sessions` 请求、排队渲染 payload 和渲染签名，防止旧缓存闪现。
- 本地缓存渲染前检查 RBAC 用户作用域，不匹配时直接清空旧列表并显示加载态。
- 修复切换账号、切出切入页面后会话列表变化的问题，`pageshow`/bfcache 恢复路径会重新同步身份并刷新会话与分享列表。
- 为会话列表增加 skeleton loading，慢请求时显示加载占位，避免空白或“选择”状态造成误判。
- 优化 admin 首屏会话加载：项目列表请求不再阻塞会话首屏渲染，并在 admin 首次会话请求上追加兼容的 `limit=120` 参数。

### 会话分享

- 增加会话分享入口，普通用户可通过会话操作菜单打开分享弹窗。
- 分享用户选择改为从 API 获取用户列表。
- 分享弹窗用户选择改为默认收起的下拉多选，支持展开、勾选、全选，并在 trigger 中显示已选用户名。
- 支持一次选择多个用户并批量提交到后端分享接口。
- 修复“收到的分享”列表缓存跨用户复用的问题，分享列表缓存按当前用户 ID 隔离。
- 分享弹窗和分享列表增加 XSS 保护，用户名、会话名等动态内容使用文本节点渲染。

### 顶栏账号菜单与登录

- 页面右上角增加用户名下拉菜单，支持切换外观、字体大小和退出登录。
- 修复退出登录后打开登录页报错的问题。
- 修复登录成功后仍保留上一个用户本地活动会话的问题，登录身份变化时清除 `hermes-webui-session`。
- 回滚登录页“用户名/密码左侧标签”布局改动，恢复为仅保留输入框的原始登录体验。

### 评分与反馈

- 修复点击五角星评分后提交按钮仍不可点击的问题。
- 增加评分控件兜底：星级点击后触发 `input/change` 事件，并解除评分弹窗/表单范围内提交按钮的禁用状态。

### License 与设置页

- 设置页用户管理增加中文兜底：用户、当前账号、添加用户、所有用户、表头、创建/删除按钮、输入占位符等英文残留会被本地化。
- License 管理页增加状态回填兜底：当页面字段仍显示 `-` 时，尝试读取 license 状态接口并回填状态、平台 ID、MAC 地址、过期时间、剩余天数、导入时间。
- License 激活页增加机器信息回填兜底：当平台 ID / MAC 地址显示 `N/A` 时，主动请求 `/api/license/machine`、`/api/license/status`、`/api/admin/license/status`、`/api/license` 并回填机器标识；接口仍不可用时显示明确错误提示。
- 增加 Python 启动期机器标识 fallback：当部署环境无法读取物理网卡 MAC 时，通过 `/sys/class/net`、`uuid.getnode()` 和稳定主机种子生成可复现的 MAC-like 标识与平台 ID，避免 License 激活页服务端渲染为 `N/A`。
- 注意：如果服务端授权检测返回 `not_activated`，页面会进入 License 激活流程。这属于 License 状态问题，不是 admin 账号本身导致。

### 验证情况

- 已完成部分轻量检查：`node --check static/sessions.js`、`python -m py_compile tests/test_session_share_ui.py`、`git diff --check` 在新增部分后续改动前曾通过。
- `./scripts/test.sh tests/test_session_share_ui.py` 受本机 License gate 影响，测试服务 `/health` 返回 403 `not_activated`，无法完成 pytest。
- 后续命令执行通道持续异常，连 `/bin/echo` 都返回 137，因此部分新增改动未能复跑自动检查。
